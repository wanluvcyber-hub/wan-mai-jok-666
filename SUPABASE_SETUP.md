# คู่มือการเชื่อมต่อ Supabase

## ตั้งค่าเสร็จแล้ว ✅

แอป Wanjot ของคุณเชื่อมต่อกับ Supabase เรียบร้อยแล้ว! นี่คือสิ่งที่ถูกตั้งค่าขึ้นมา:

### ไฟล์ที่สร้าง:
1. **`.env.local`** - ตัวแปรสภาพแวดล้อม (ห้ามบันทึกลงใน git)
2. **`src/lib/supabase.ts`** - เริ่มต้นไคลเอนต์ Supabase
3. **`src/lib/supabase-helpers.ts`** - ฟังก์ชันช่วยสำหรับการดำเนินการ CRUD
4. **`src/hooks/useTransactions.ts`** - React hook สำหรับจัดการรายการ
5. **`src/components/TransactionDialog.tsx`** - คอมโพเนนต์แบบฟอร์มสำหรับเพิ่ม/แก้ไข
6. **`src/routes/list.tsx`** - อัปเดตให้ใช้ข้อมูล Supabase จริง

### คุณลักษณะที่ใช้งาน:
✅ **เพิ่ม** - คลิกปุ่ม + เพื่อเพิ่มรายการใหม่
✅ **แก้ไข** - นำเมาส์ไปวางบนรายการแล้วคลิกไอคอนแก้ไข
✅ **ลบ** - นำเมาส์ไปวางบนรายการแล้วคลิกไอคอนลบ
✅ **เรียลไทม์** - การเปลี่ยนแปลงซิงค์ทันทีโดยใช้ Supabase subscriptions
✅ **แดชบอร์ด** - แสดงสถิติแบบสดใจมาจากข้อมูล Supabase
✅ **การกรอง** - กรองตามรายรับ/รายจ่าย

### สคีมาตาราง Supabase:
แอปคาดว่าจะมีตาราง `transactions` ที่มีคอลัมน์เหล่านี้:
```
- id (uuid, primary key)
- date (text/date - ISO format YYYY-MM-DD)
- time (text - HH:MM format)
- title (text)
- category (text)
- amount (numeric - ติดลบสำหรับรายจ่าย, ค่าบวกสำหรับรายรับ)
- created_at (timestamp, ไม่จำเป็น)
- updated_at (timestamp, ไม่จำเป็น)
```

### วิธีใช้:

#### 1. เพิ่มรายการ:
- คลิกปุ่มสีชมพู + ที่มุมขวาล่าง
- กรอกแบบฟอร์ม:
  - ชื่อเรื่อง (เช่น "ค่าน้ำ")
  - หมวดหมู่ (จากรายการเลือก)
  - จำนวนเงิน (เพิ่มเครื่องหมายอัตโนมัติตามหมวดหมู่รายรับ/รายจ่าย)
  - วันที่และเวลา
- คลิก "เพิ่มรายการ"

#### 2. แก้ไขรายการ:
- นำเมาส์ไปวางบนรายการในรายการ
- คลิกไอคอน ✏️ (แก้ไข)
- แก้ไขรายละเอียด
- คลิก "บันทึกการเปลี่ยนแปลง"

#### 3. ลบรายการ:
- นำเมาส์ไปวางบนรายการใด ๆ
- คลิกไอคอน 🗑️ (ลบ)
- ยืนยันการลบในกล่องโต้ตอบ

### การซิงค์แบบเรียลไทม์:
แอปใช้ Supabase real-time subscriptions ดังนั้น:
- เมื่อคุณเพิ่ม/แก้ไข/ลบบนอุปกรณ์นี้ UI จะอัปเดตทันที
- หากคุณทำการเปลี่ยนแปลงจากอุปกรณ์/เบราว์เซอร์อื่น แอปนี้จะซิงค์โดยอัตโนมัติ
- ไม่จำเป็นต้องรีเฟรชด้วยตนเอง!

### Hooks และฟังก์ชันที่ใช้ได้:

#### Hook `useTransactions()`:
```typescript
const {
  transactions,      // อาร์เรย์ของรายการทั้งหมด
  loading,           // สถานะการโหลด
  error,            // ข้อความข้อผิดพลาดถ้ามี
  addTransaction,   // ฟังก์ชันแบบอะซิงก์เพื่อเพิ่ม
  updateTransaction, // ฟังก์ชันแบบอะซิงก์เพื่ออัปเดต
  deleteTransaction, // ฟังก์ชันแบบอะซิงก์เพื่อลบ
  refetch,          // ฟังก์ชันแบบอะซิงก์เพื่อดึงข้อมูลอีกครั้ง
} = useTransactions();
```

#### ฟังก์ชันช่วยเหลือ (src/lib/supabase-helpers.ts):
```typescript
// ดึงข้อมูลทั้งหมดจากตาราง
await fetchData('transactions')

// แทรกระเบียนใหม่
await insertData('transactions', {title: 'test', ...})

// อัปเดตระเบียน
await updateData('transactions', id, {title: 'updated'})

// ลบระเบียน
await deleteData('transactions', id)

// สมัครรับการเปลี่ยนแปลงแบบเรียลไทม์
const channel = subscribeToTable('transactions', (payload) => {
  console.log('Changes:', payload)
})
```

### แก้ไขปัญหา:

**ค: รายการไม่ปรากฏ?**
- ตรวจสอบให้แน่ใจว่าตาราง `transactions` มีอยู่ใน Supabase
- ตรวจสอบว่า `.env.local` มี VITE_SUPABASE_URL และ VITE_SUPABASE_ANON_KEY ที่ถูกต้อง
- เปิดคอนโซลเบราว์เซอร์เพื่อดูข้อผิดพลาดใด ๆ

**ค: การเปลี่ยนแปลงไม่ซิงค์?**
- ตรวจสอบแท็บเครือข่าย - ตรวจสอบว่าคำขอ Supabase สำเร็จ
- ยืนยันว่านโยบาย Row Level Security (RLS) อนุญาต INSERT/UPDATE/DELETE
- ลองรีเฟรชหน้าหากเรียลไทม์ไม่ทำงาน

**ค: แบบฟอร์มไม่ปรากฏ?**
- ตรวจสอบให้แน่ใจว่าคอมโพเนนต์กล่องโต้ตอบนำเข้าถูกต้อง
- ตรวจสอบคอนโซลเพื่อหาข้อผิดพลาดการนำเข้า

### ขั้นตอนถัดไป:

1. ✅ สร้างตาราง `transactions` ใน Supabase
2. ✅ เพิ่มข้อมูลทดสอบเริ่มต้น (ไม่จำเป็น)
3. ✅ ทดสอบฟังก์ชันเพิ่ม/แก้ไข/ลบ
4. ✅ ตรวจสอบการซิงค์เรียลไทม์โดยการเปิดหน้าต่างเบราว์เซอร์สองหน้า
5. ขยายด้วยคุณลักษณะเพิ่มเติม (จัดการหมวดหมู่, งบประมาณ ฯลฯ)

เพลิดเพลินไปกับการใช้ Wanjot กับข้อมูล Supabase แบบสด! 🎉
